spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: dupa
  jpa:
    hibernate:
      ddl-auto: create-drop   # każdorazowo tworzy schemat od nowa
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
server:
  servlet:
    context-path: /
  port: 8081
